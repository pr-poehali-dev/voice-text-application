{
  "tests": [
    {
      "name": "CORS preflight",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200
    },
    {
      "name": "GET без payment_id — ЮKassa не настроена (500)",
      "method": "GET",
      "path": "/",
      "expectedStatus": 500,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}